{
  "compilerOptions": {
    "target": "es2023",                    // Версия JavaScript в которую компилируем
    "module": "esnext",                  // Используем CommonJS для Node.js (НЕ ESNext!)
    "moduleResolution": "node",            // Разрешение модулей по правилам Node.js
    "outDir": "./dist",                    // Папка для скомпилированного JS
    "rootDir": "./src",                    // Корневая папка исходников
    "strict": true,                        // Все строгие проверки типов
    "esModuleInterop": true,              // Совместимость ES/CommonJS модулей
    "allowSyntheticDefaultImports": true,  // Позволяет import bot from "./bot"
    "skipLibCheck": true,                 // Пропускает проверку типов в node_modules
    "forceConsistentCasingInFileNames": true, // Проверяет регистр в именах файлов
    "resolveJsonModule": true,            // Позволяет импортировать JSON файлы
    "allowImportingTsExtensions": false,  // Запрещаем .ts в импортах
    "declaration": true,                  // Генерирует .d.ts файлы
    "sourceMap": true                     // Генерирует source maps для отладки
  },
  "include": [
    "src/**/*"                           // Включить все файлы из src
  ],
  "exclude": [
    "node_modules",                      // Исключить node_modules
    "dist"                              // Исключить папку с результатами компиляции
  ]
}